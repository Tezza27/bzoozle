//This class contains the data required to build the buttons on the main menu screen
class MenuButtonClass {
  const MenuButtonClass({this.buttonText, this.routerName, this.buttonImage});

  final String buttonText;
  final String routerName;
  final String buttonImage;
}

import 'package:flutter/material.dart';

class CountdownViewerScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            "This screen will contain the selected 'countdown to their next trip to Vegas' graphic that they have created.  It will also contain the facilities to easily share the graphic on their own social media pages"),
      ),
    );
  }
}

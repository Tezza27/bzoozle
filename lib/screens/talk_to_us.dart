import 'package:flutter/material.dart';

class TalkToUsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            "This screen will contain a form for the user to complete to send their feedback on the app"),
      ),
    );
  }
}

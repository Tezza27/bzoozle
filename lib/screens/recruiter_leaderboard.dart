import 'package:flutter/material.dart';

class RecruiterLeaderboardScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            "This screen will contain the a leader board ranking users who have recruited new users, to encourage virality"),
      ),
    );
  }
}

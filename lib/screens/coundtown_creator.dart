import 'package:flutter/material.dart';

class CountdownCreatorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            "This screen will contain the tools to allow the user to construct a 'countdown to their next trip to Vegas' graphic which can be shared on their own social media pages"),
      ),
    );
  }
}

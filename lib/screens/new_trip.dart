import 'package:flutter/material.dart';

class NewTripScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            "This screen will contain a small for for the users to enter details of their upcoming Vegas trips, which will be used in the creation of their countdown graphics"),
      ),
    );
  }
}

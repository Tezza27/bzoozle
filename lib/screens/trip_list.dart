import 'package:flutter/material.dart';

class TripListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
            "This screen will contain the list of trips to Vegas that the user has created"),
      ),
    );
  }
}

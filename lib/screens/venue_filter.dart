import 'package:flutter/material.dart';

//This screen gives the user the ability to filter the venue list to exclude the
//venues that they are not interested in
class VenueFilterScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Column(
          children: <Widget>[],
        ),
      ),
    );
  }
}
